appId: com.oicar.mobileapp
---
# 🧪 E2E Test: Item Browsing Flow
# Tests: Product discovery → API data → UI rendering

- launchApp
- assertVisible: "OICAR"

# Navigate to items/marketplace
- tapOn: "Browse Items"
- assertVisible: "Marketplace"

# Wait for items to load from API
- assertVisible:
    text: "Loading"
    timeout: 3000
    
# Verify items loaded from database
- assertVisible: "Items found"
- assertVisible: "$"  # Price indicator

# Test search functionality
- tapOn: "Search"
- inputText: "car"
- tapOn: "Search Button"
- assertVisible: "Search results"

# Test item details
- tapOn: 
    text: "View Details"
    index: 0
- assertVisible: "Item Details"
- assertVisible: "Price:"
- assertVisible: "Description:"
- assertVisible: "Seller:"
- assertVisible: "Add to Cart"

# Test image gallery (if available)
- scrollDown
- assertVisible: "Photos"

# Test back navigation
- tapOn: "Back"
- assertVisible: "Marketplace"

# Test category filtering
- tapOn: "Categories"
- tapOn: "Electronics"
- assertVisible: "Electronics"
- assertVisible: "Filter applied"

# Clear filters
- tapOn: "Clear Filters"
- assertVisible: "All Items"

# Test sorting
- tapOn: "Sort"
- tapOn: "Price: Low to High"
- assertVisible: "Sorted by Price"

# Verify pagination/infinite scroll
- scrollDown
- scrollDown
- assertVisible: "Loading more items"

---
# 🎯 What this test validates:
# ✅ Items API endpoint (/api/items) integration
# ✅ Real product data from Azure SQL Database
# ✅ Search functionality (client-side and API)
# ✅ Category filtering and sorting
# ✅ Item details navigation
# ✅ Image loading and display
# ✅ Pagination/infinite scroll performance
# ✅ UI responsiveness with real data volumes 